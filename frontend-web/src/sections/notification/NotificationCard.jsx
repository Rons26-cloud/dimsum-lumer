import { ChevronRight, Trash2 } from "lucide-react";
import NotificationIcon from "../../components/notification/NotificationIcon.jsx";
import NotificationStatus from "../../components/notification/NotificationStatus.jsx";
import NotificationTime from "../../components/notification/NotificationTime.jsx";
export default function NotificationCard({ item, onOpen, onNavigate, onDelete }) {
  return <article className={`group flex items-center gap-3 rounded-3xl border bg-white p-3 shadow-sm transition-all ${item.is_read?'border-gray-100':'border-primary/20 ring-1 ring-primary/5'}`}><NotificationIcon type={item.type} read={item.is_read}/><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><button onClick={()=>onNavigate(item)} className="line-clamp-1 text-left text-xs font-extrabold text-dark transition-colors hover:text-primary" aria-label={`Buka ${item.title || 'notifikasi'}`}>{item.title || 'Dimsum Lumer'}</button><NotificationStatus read={item.is_read}/></div><button onClick={()=>onOpen(item)} className="block w-full text-left"><span className="mt-1 line-clamp-2 block text-[10px] leading-relaxed text-gray-500">{item.message}</span><span className="mt-2 flex items-center justify-between"><NotificationTime value={item.created_at}/><ChevronRight size={15} className="text-primary"/></span></button></div><button onClick={()=>onDelete(item)} className="grid h-8 w-8 shrink-0 place-items-center rounded-full text-gray-300 hover:bg-red-50 hover:text-red-500" aria-label={`Hapus ${item.title}`}><Trash2 size={15}/></button></article>;
}
