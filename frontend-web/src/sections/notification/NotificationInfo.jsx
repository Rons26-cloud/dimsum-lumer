const row=(label,value)=><div className="flex justify-between gap-4 border-b border-gray-50 py-2.5 last:border-0"><span className="text-[10px] text-gray-400">{label}</span><strong className="max-w-[65%] text-right text-[10px] text-dark">{value||'-'}</strong></div>;
export default function NotificationInfo({ notification, order }) { const meta=notification.metadata||{};return <section className="rounded-3xl border border-gray-100 bg-white p-4 shadow-sm"><h2 className="mb-1 text-xs font-extrabold">Informasi Pesanan</h2>{row('Nomor Pesanan',order?.order_code||order?.id)}{row('Status',order?.status)}{row('Estimasi Tiba',meta.estimated_arrival)}{row('Driver',meta.driver_name)}{row('Kendaraan',[meta.vehicle_type,meta.vehicle_number].filter(Boolean).join(' · '))}{row('Telepon',meta.driver_phone)}{row('Toko',meta.store_name)}{row('Pembayaran',order?.payment_method)}</section>; }
