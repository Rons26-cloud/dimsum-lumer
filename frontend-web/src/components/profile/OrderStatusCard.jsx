export default function OrderStatusCard({ icon:Icon,label,count,onClick }) { return <button onClick={onClick} className="flex min-w-0 flex-col items-center gap-1 rounded-xl py-1.5 text-center active:bg-primary-50"><span className="relative grid h-8 w-8 place-items-center rounded-xl bg-gray-50 text-dark active:text-primary"><Icon size={15}/>{count>0&&<b className="absolute -right-1 -top-1 grid min-h-3.5 min-w-3.5 place-items-center rounded-full bg-primary px-1 text-[7px] text-white">{count}</b>}</span><span className="w-full truncate text-[7px] font-semibold text-dark">{label}</span></button>; }
