import { Camera, UserRound } from "lucide-react";
export default function ProfileAvatar({ src, name, size="lg", editable=false, onChange }) { const dimensions=size==='xl'?'h-16 w-16':'h-12 w-12';return <label className={`relative grid shrink-0 place-items-center overflow-hidden rounded-full border-2 border-primary/10 bg-primary-50 text-primary ${dimensions} ${editable?'cursor-pointer':''}`}>{src?<img src={src} alt={name||'Foto profil'} className="h-full w-full object-cover"/>:<UserRound size={size==='xl'?27:21}/>} {editable&&<><input type="file" accept="image/jpeg,image/png,image/webp" className="sr-only" onChange={(event)=>event.target.files?.[0]&&onChange(event.target.files[0])}/><span className="absolute bottom-0 right-0 grid h-6 w-6 place-items-center rounded-full bg-primary text-white shadow"><Camera size={11}/></span></>}</label>; }
