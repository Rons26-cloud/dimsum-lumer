import { Download, RefreshCw, Upload } from "lucide-react";
import Modal from "../ui/Modal.jsx";

const Switch=({checked,onChange})=><button type="button" role="switch" aria-checked={checked} onClick={()=>onChange(!checked)} className={`relative h-7 w-12 rounded-full transition ${checked?"bg-green-500":"bg-gray-300"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${checked?"left-6":"left-1"}`}/></button>;
export default function ApkReleaseModal({ open, apk, file, saving, onClose, onApkChange, onFileChange, onSubmit }) {
  return <Modal open={open} title="Kelola APK" onClose={()=>!saving&&onClose()}><form onSubmit={onSubmit} className="space-y-4"><label className="block text-sm font-medium">Nomor versi<input required pattern="[0-9]+\.[0-9]+\.[0-9]+" value={apk.version||""} onChange={(event)=>onApkChange({...apk,version:event.target.value})} placeholder="1.0.3" className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-medium">Direct link APK<input type="url" value={apk.download_url||""} onChange={(event)=>onApkChange({...apk,download_url:event.target.value})} placeholder="https://.../app.apk" className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-medium">Catatan rilis<textarea value={apk.release_notes||""} onChange={(event)=>onApkChange({...apk,release_notes:event.target.value})} rows={3} placeholder="Perbaikan dan fitur pada versi ini" className="mt-1 w-full resize-none rounded-xl border p-3"/></label><label className="block rounded-xl border-2 border-dashed p-4 text-center text-sm"><Upload className="mx-auto mb-2 text-gray-400"/>Upload file APK<input type="file" accept=".apk,application/vnd.android.package-archive" onChange={(event)=>onFileChange(event.target.files?.[0]||null)} className="mt-2 block w-full text-xs"/>{file&&<span className="mt-2 block truncate text-xs text-gray-500">{file.name}</span>}</label><div className="flex items-center justify-between"><div><p className="text-sm font-semibold">Wajibkan user update</p><p className="text-xs text-gray-500">Dialog tidak dapat ditutup.</p></div><Switch checked={Boolean(apk.force_update)} onChange={(force_update)=>onApkChange({...apk,force_update})}/></div><button disabled={saving||(!file&&!apk.download_url)} className="flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3 font-bold text-white disabled:opacity-50">{saving?<RefreshCw className="animate-spin" size={16}/>:<Download size={16}/>}Terbitkan Versi</button></form></Modal>;
}
